# Day 1 - Notes API

This is my first backend project built using Node.js + Express.
It includes:

- GET all notes
- GET note by ID
- POST a note
- PUT (update) a note
- DELETE a note

## How to Run
1. Clone the repo
2. Run `npm install`
3. Run `node index.js`
4. Test endpoints with Postman
